export interface Product {
  id: {
    type: string;
  };
  title: {
    type: string;
  };
  category: {
    type: string;
  };
  description: {
    type: string;
  };
  price: {
    type: number;
  };
  image: {
    type: string;
  };
}
